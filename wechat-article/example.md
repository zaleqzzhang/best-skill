---
title: 微信公众号自动化发布完全指南
cover: ./example-cover.png
author: wechat-toolkit
---

# 微信公众号自动化发布完全指南

你是否还在为公众号发文而烦恼？手动排版、逐张上传图片、反复粘贴到后台……这些重复劳动完全可以自动化。

本文介绍 **wechat-toolkit** — 一个专为微信公众号设计的自动化发布工具包，让你用 Markdown 写作，一键发布到公众号。

## 为什么需要自动化？

手动发布公众号文章的痛点相信你深有体会：

- 排版耗时：在编辑器里反复调整样式
- 图片麻烦：一张张上传，还要处理尺寸
- 流程繁琐：复制粘贴、预览、修改、再预览
- 格式丢失：从 Markdown 粘贴后样式全乱

> 自动化不是偷懒，而是把时间花在真正重要的事情上——内容创作。

## 核心功能一览

wechat-toolkit 提供三个核心能力：

| 功能 | 说明 | 命令 |
|------|------|------|
| 发布文章 | 将 HTML 发布到公众号草稿箱 | `node scripts/publisher/publish.js` |
| 上传图片 | 上传图片到永久素材库 | `node scripts/publisher/upload_image.js` |
| 内联图片 | 自动上传正文中的本地图片 | 随发布流程自动触发 |

## 快速上手

### 第一步：配置凭证

在开始使用前，需要设置微信公众号的 API 凭证：

```bash
export WECHAT_APP_ID=your_app_id
export WECHAT_APP_SECRET=your_app_secret
```

获取方式：登录 [微信公众平台](https://mp.weixin.qq.com) → 开发 → 基本配置。

### 第二步：编写文章

使用熟悉的 Markdown 语法撰写文章。支持标准的标题、段落、列表、代码块、表格等语法。

### 第三步：AI 智能排版

将 Markdown 文件交给 AI，自动转换为适配微信公众号的 HTML：

- 所有样式内联（inline style）
- 手机端优先的阅读体验
- 代码块语法高亮
- 自适应图片宽度

### 第四步：一键发布

```bash
# 发布到草稿箱
node ./scripts/publisher/publish.js article.html
```

脚本会自动完成：上传内联图片 → 生成封面 → 提取摘要 → 发布到草稿箱。

## 技术细节

### 图片处理机制

发布脚本会自动扫描正文中的本地图片路径，上传到微信永久素材库，并将 URL 回填替换。整个过程无需手动干预。

支持的处理流程如下：

1. 解析 HTML 中所有 `<img>` 标签
2. 判断 src 是否为本地文件路径
3. 调用微信 API 上传图片
4. 用返回的 URL 替换原始路径
5. 输出最终可发布的 HTML

### 封面图策略

封面图通过 frontmatter 的 `cover` 字段指定：

```markdown
---
cover: /absolute/path/to/cover.jpg
---
```

如果未指定封面图，脚本会自动生成一张 900×500 的蓝色默认封面，确保不会因为缺失封面而发布失败。

## 常见问题

### Q：IP 不在白名单怎么办？

运行以下命令查看当前公网 IP：

```bash
curl ifconfig.me
```

然后将输出的 IP 地址添加到微信公众号后台的白名单中。

### Q：支持哪些图片格式？

目前支持 jpg、jpeg、png、gif、bmp、webp 六种格式，单张图片大小不超过 10MB。

### Q：永久素材库有上限吗？

有，微信永久素材库的图片上限为 5000 张。建议定期清理不再使用的图片资源。

### 测试 code

```
# ============================================
#   AGENT CODING RULES
#   四大铁律：让 AI 输出更加完美
# ============================================

## 一、编码前先思考（Think Before Coding）
# 核心原则：不做假设、不隐藏困惑、直面取舍
#
# 在写代码前，必须做到：
# - 明确列出你的所有假设：有不确定的地方，直接提问，不要瞎猜
# - 存在多种理解时，全部列出：有歧义就展示所有可能性，
#   不要私自做决定
# - 有更简单的方案，直接提出：没必要硬做复杂实现
# - 搞不懂就立刻停下：说清哪里不明确，向用户确认
#
# 目的：消除 AI「自作主张、幻觉编码、偷偷做决定」的问题

## 二、优先极简实现（Simplicity First）
# 核心原则：只写刚好解决问题的最少代码，
#           拒绝任何多余的猜测性实现
#
# 杜绝 AI 最容易犯的「过度设计」问题：
# - 不要添加需求之外的任何功能
# - 一次性使用的代码，不要搞复杂抽象
# - 未被要求的「灵活性、可配置性」一律不做
# - 不为不可能发生的场景写错误处理
# - 200 行代码能缩到 50 行，直接重写
#
# 判断标准：
#   资深工程师看了会不会觉得复杂？
#   会，就简化。

## 三、精准局部改动（Surgical Changes）
# 核心原则：只碰必须改的代码，不动任何无关内容
#
# 规则约束：
# - 只修改和当前任务直接相关的文件、代码行
# - 不要顺手重构、格式化、优化相邻代码
# - 不改动无关注释、不清理历史遗留的死代码
# - 只删除你自己新增后废弃的代码，原有代码一律不动
#
# 黄金标准：
#   你每一行改动，都能直接对应到用户的需求

## 四、目标驱动执行（Goal-Driven Execution）
# 核心原则：先定义成功标准，循环迭代直到验证通过
#
# - 把模糊的用户指令，转化为可验证的具体目标
#   例：「加个表单验证」→
#       「为空输入时返回报错、数字输入非法时报错」
# - 多步骤任务，先写极简执行计划
# - 每完成一步，立刻验证是否达标，达标再进入下一步
# - 全程保持上下文简洁，不做额外延伸开发
```

## 总结

微信公众号自动化发布的完整链路可以概括为：

```
Markdown 写作 → AI 智能排版 → 一键发布 → 草稿箱待审
```

整个过程中，你只需要专注于内容创作，剩下的交给工具就好。

---

*由 wechat-toolkit 生成并发布*
