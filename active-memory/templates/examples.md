# 记录示例与质量自检

## 当日日志示例

### ✅ 正确示例

```markdown
## 库存排序方案 - 已确认
- ✅ 方案A：新建 StockSortingOptimizeService，七彩石开关控制新旧逻辑
- ❌ 方案B 否决：排序和对冲是不同关注点，不应混合
```

```markdown
## 对冲流程 - 架构认知
- ✅ 排序在策略层(singleStrategyExecuteHedge)，对冲在对冲层(doHedgeForPnStock)
- ⏳ 增量排序优化应放在策略层实现
```

```markdown
## OOM 排查 - 已修复
- ✅ 根因：循环内创建大对象导致 GC 压力
- ✅ 修复：改为对象池复用，涉及 DataProcessor.java
```

### ❌ 错误示例（禁止）

```markdown
### 库存排序算法方案确认
- **用户决策**: ✅ 确认方案A - 新建独立排序服务类
- **方案详情**: 新建 StockSortingOptimizeService，通过七彩石开关控制新旧逻辑
- **选择理由**: 向前兼容，风险可控
- **下一步**: ⏳ 开始详细设计和编码
```
> 问题：使用 `###` 子标题、每行带粗体标签、超过 3 行

## 长期记忆示例

### ✅ 正确示例

```markdown
## 库存排序 - 新建独立服务类 (2026-03-11)
新建 StockSortingOptimizeService，七彩石开关控制。排序在策略层，对冲在对冲层。
```

```markdown
## 数据库选型 - MySQL + Redis (2026-03-12)
MySQL 8.0 主库 + Redis 7.0 缓存。事务一致性 + 高并发缓存需求。
```

```markdown
## 库存排序 - ❌ 方案B 否决 (2026-03-11)
在对冲层做排序调整被否决：排序和对冲是不同关注点。
```

### ❌ 错误示例（禁止）

```markdown
## 库存排序算法优化 - 设计决策 (2026-03-11)
### 核心方案: 新建 StockSortingOptimizeService，通过七彩石开关控制
### 关键架构认知: 排序在策略层，对冲在对冲层
### 用户确认决策: 增量排序位置在策略层，型号级别判断增量/存量
```
> 问题：使用 `###` 子标题展开、总长度远超 100 字

## 质量自检【推荐】

记录后自问：

| 检查项 | 标准 |
|--------|------|
| 行数 | 日志 ≤ 3 行？MEMORY.md ≤ 100 字？ |
| 结论优先 | 只记结论，没有过程/推理？ |
| 状态标记 | 有 ✅/❌/⏳ 标记？ |
| 无重复 | MEMORY.md 中没有同主题重复条目？ |
